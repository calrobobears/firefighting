#include "sensors.h"
#include "controls.h"

void setup()
{    
    //Wait for alarm
    //Find fire
    //Extinguish fire
    //Go home
}

/* We want to be able to control the robot with a linear sequence of instructions
 * ie, wait for alarm, find fire, put out fire, go home.
 * This is easier if we loop manually in the setup function instead of using the loop function
 */
void loop(){}
